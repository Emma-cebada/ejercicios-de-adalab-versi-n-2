// Esto es el código que tenemos en el ejemplo y que tengo que reescribir utilizando un ternario.

let avocados;
const avocadoPrice = 1.5;
const money = 33;

avocados(money >= avocadoPrice ? "money / avocadoPrice" : "0");


// if (money >= avocadoPrice) {
//   avocados = money / avocadoPrice;
// } else {
//   avocados = 0;
// }
